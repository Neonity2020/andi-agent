{
  "name": "andi-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test",
    "test:live": "bun run src/cli.ts --cwd . \"Use read_file to find the package name, then use run_command to run the typecheck script. Do not modify files. Report the package name and whether typecheck passed.\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.0"
  }
}
